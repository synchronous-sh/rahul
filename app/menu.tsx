import { Ionicons } from '@expo/vector-icons';
import { router } from 'expo-router';
import { Pressable, SafeAreaView, StyleSheet, Text, View } from 'react-native';
import { colors } from '@/constants/theme';
import { goBack } from '@/lib/navigation';
const rows = [{ icon: 'options-outline', label: 'Manage interests', route: '/onboarding' }, { icon: 'bookmarks-outline', label: 'Saved ideas', route: '/settings/saved-ideas' }, { icon: 'download-outline', label: 'Downloads', route: '/settings/downloads' }, { icon: 'settings-outline', label: 'Settings', route: '/settings' }] as const;
export default function Menu() { return <SafeAreaView style={styles.screen}><View style={styles.nav}><Pressable onPress={() => goBack()} style={styles.back}><Ionicons name="chevron-back" size={27} color="#fff" /></Pressable><Text style={styles.title}>Menu</Text><View style={styles.back} /></View><View style={styles.content}>{rows.map(row => <Pressable key={row.label} style={styles.row} onPress={() => router.push(row.route)}><Ionicons name={row.icon} size={21} color="#fff" /><Text style={styles.label}>{row.label}</Text><Ionicons name="chevron-forward" size={18} color={colors.secondary} /></Pressable>)}</View></SafeAreaView>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: '#000' }, nav: { height: 54, paddingHorizontal: 10, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', borderBottomWidth: 1, borderBottomColor: colors.border }, back: { width: 44, height: 44, alignItems: 'center', justifyContent: 'center' }, title: { color: '#fff', fontSize: 15, fontWeight: '600' }, content: { padding: 20 }, row: { height: 62, flexDirection: 'row', alignItems: 'center', gap: 14, borderBottomWidth: 1, borderBottomColor: colors.border }, label: { color: '#fff', fontSize: 16, flex: 1 } });
